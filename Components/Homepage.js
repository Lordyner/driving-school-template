import React from 'react';
import classes from './Homepage.module.css';

const Homepage = () => {
    return (
        <section id="main-section" className={classes.mainContent}>
            <h1>Homepage</h1>
        </section>
    );
};

export default Homepage;